Terminator = CR LF;

getCurrentControlChannel {
    out "Q300";
	in "%d";
}

setCurrentControlChannel {
    out "C300,%d";
	@init { getCurrentControlChannel; }
}

getWatchdogStatus {
    out "Q904";
	in "%d%*s";
}

setWatchdogStatus {
	out "C904,%d,0";
	@init { getWatchdogStatus; }
}

